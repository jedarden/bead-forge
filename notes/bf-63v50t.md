# bf-63v50t — Epic Test 1 (label search)

**Type:** `epic` | **Labels:** `backend`, `epic-test` | **Status:** in_progress → closed
**Task:** "Test epic for label search" — verification-only, no code task.

## What this bead is

A test epic created to exercise the label-search path. Like the other recent
label/epic test beads (bf-5voa30, bf-2spaoy, bf-3fkwv…), there is no
implementation work — only verification that an epic carrying labels is
correctly stored and retrievable by label.

## Verification (all pass)

| Check | Result |
|-------|--------|
| `bf labels bf-63v50t` | `backend`, `epic-test` ✓ |
| br-compatible `labels` table rows | `bf-63v50t\|backend`, `bf-63v50t\|epic-test` ✓ |
| `bead_labels` (bf-specific) table | empty — labels live in br-compat `labels` table |
| JSONL checkpoint (git-tracked) | `"labels":["backend","epic-test"]` persisted ✓ |
| `bf search -l epic-test` | finds bf-63v50t ✓ |
| `bf search -l backend` | finds bf-63v50t ✓ (needs `--limit` > default 50; it sits past rank 50) |
| `bf search -l epic-test -t epic` (type+label combo) | finds bf-63v50t (in_progress) + bf-5vz3z6 (open) ✓ |
| `sqlite3 .beads/beads.db "PRAGMA integrity_check;"` | `ok` ✓ |

## Notes

- Label data is stored in the **br-compatible `labels` table** (`issue_id`,
  `label`), not the bf-specific `bead_labels` table. Search reads from the
  br-compat table, so it interops with `br` checkpoints.
- The initial "missing from backend search" impression was a false alarm: the
  `bf search` default `--limit` is 50, and this bead ranks beyond 50 under the
  `backend` label. With `--limit 1000` (179 backend-labelled beads total) it is
  present.
- JSONL checkpoint still shows `status:"open"` while the live store shows
  `in_progress` — expected: the checkpoint lags the live store between
  auto-flushes. The label array itself is consistent across both.
