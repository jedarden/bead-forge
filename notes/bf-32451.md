# bf-32451 — Triage untracked trace/notes files (test artifacts vs. real work)

**Date:** 2026-07-22
**Verdict:** Every untracked trace is **real work → KEEP**. The **drop set is empty**.
Zero untracked `notes/bf-*.md` exist (all 337 notes are already tracked).

## 1. What is actually untracked

Captured with `git ls-files --others --exclude-standard`:

| Surface | Count | Detail |
|---|---|---|
| `notes/bf-*.md` (untracked) | **0** | All 337 `notes/*.md` are tracked. The task's "untracked notes" premise was stale. |
| `.beads/traces/bf-*/` dirs (untracked) | **16** | `bf-32451 bf-3c75p bf-3cu1k bf-3hm5h bf-4gkg5 bf-4iqxz bf-4waen bf-5bp7f bf-5wku bf-5wz0l bf-5y3cj bf-61nxm bf-fwx5y bf-gj673 bf-j7w7 bf-yrqmr` |
| Standalone new `trace.jsonl` in *tracked* dirs | **2** | `.beads/traces/bf-1wj/trace.jsonl`, `.beads/traces/bf-e8hh3/trace.jsonl` (their metadata/stdout were already tracked → modified) |
| **Total untracked trace files** | **45** | matches the task's "45+" |

## 2. Per-trace classification — all KEEP

| Bead | Title (abbrev.) | Status | Agent / provider | Outcome | Verdict |
|---|---|---|---|---|---|
| bf-3c75p | Flush workspace + review issues.jsonl | closed | glm-5 / zai | success | **keep** |
| bf-3cu1k | Write bf-checkpoint.sh flush/diff/commit script | in_progress | glm-5 / zai | success | **keep** (active) |
| bf-3hm5h | 7.3 NULL-datetime & schema hardening | closed | opus / anthropic | success | **keep** |
| bf-4gkg5 | Scaffold bf-checkpoint.sh config gating | closed | glm-5 / zai | success | **keep** |
| bf-4iqxz | Confirm no active NEEDLE workers hold uncommitted state | closed | glm-5 / zai | success | **keep** |
| bf-4waen | Port orphan-file scenarios into tests/*.rs | closed | glm-5 / zai | success | **keep** |
| bf-5bp7f | Add epic CLI label sorting and filter tests | closed | glm-5 / zai | success | **keep** |
| bf-5wku | Implement formatter for search/claim commands | in_progress | glm-5 / zai | success | **keep** (active) |
| bf-5wz0l | Audit orphan-file scenario coverage | closed | glm-5 / zai | success | **keep** |
| bf-5y3cj | Interval throttle, opt-in push, deploy/scripts parity | closed | glm-5 / zai | success | **keep** |
| bf-61nxm | Stage-only-issues.jsonl commit path, fixed git identity | closed | glm-5 / zai | success | **keep** |
| bf-fwx5y | Verify test_version binary/source safe to remove | in_progress | (in-flight, metadata empty) | — | **keep** (active) |
| bf-gj673 | Fix assignee-clearing gap in create/update/reopen | in_progress | opus / anthropic | timeout | **keep** (active) |
| bf-j7w7 | Fix claim command JSON output to use formatter | in_progress | glm-5 / zai | success | **keep** (active) |
| bf-yrqmr | Add flush + git change detection to bf-checkpoint.sh | closed | glm-5 / zai | success | **keep** |
| bf-1wj | bf ready --format json omits assignee/labels (regression) | closed | — | — | **keep** (new trace.jsonl) |
| bf-e8hh3 | Fix overbroad Azure Key secret-scanner regex | closed | — | — | **keep** (new trace.jsonl) |

## 3. Why none are test artifacts (5 independent checks)

1. **Every trace ID resolves to a real bead** in `.beads/issues.jsonl` (1 hit each) with a substantive, non-"Test …" title.
2. **Every `metadata.json` shows a genuine needle-dispatch agent** — `claude-code-glm-5`/`zai`/`glm-5` or `claude-print-opus`/`anthropic`/`opus` — with realistic durations (137 s–1200 s). Test scripts produce no such metadata.
3. **No trace bead ID is referenced by any `test_*.sh`** (`grep` across `test_*.sh` and `tests/`).
4. **`test_*.sh` scripts self-isolate in `mktemp -d`** (e.g. `test_bf_create.sh`: `TEST_DIR=$(mktemp -d); cd "$TEST_DIR"; mkdir -p .beads`). They never write into the production `.beads/traces/` tree, so they cannot be the source of these traces.
5. **Traces are emitted only by the agent-dispatch harness**, never by a bare `bf create` call — which is all the test scripts do.

## 4. Cross-reference to bf-20ozg ("Isolate test_*.sh scripts")

bf-20ozg's *test-artifact* signature is **junk beads** (in `issues.jsonl`, *not* traces): titles like
`Test epic creation` (bf-s9tt7), `Test Epic 1` (bf-127ow), `Comprehensive epic P1 test` (bf-kd343),
created by `test_epic_type_creation.sh` / `test_p0_epic_creation.sh` which omit the `mktemp -d` isolation.

**None of the 16 untracked trace beads match that signature** — confirming bf-20ozg's junk-bead
problem is disjoint from these traces. bf-20ozg's cleanup (isolating the two scripts + closing junk
beads) is separate work; it does not touch this trace set.

## 5. Explicit stage vs. exclude sets

- **DROP / exclude (test artifact):** `∅` (empty). Nothing to discard.
- **KEEP / stage (real work):** all 45 untracked trace files under `.beads/traces/bf-*/`.

### Who stages them — scoping note (shared workspace)

This working tree is shared by many concurrent NEEDLE workers ([[bead-forge-shared-workspace-races]]):
commit single paths, never `git add -A`. The traces are not committed by *this* triage bead because:

- There is a dedicated catch-up bead — **bf-33zhy** ("One-time catch-up: flush and commit bead-forge's
  own uncommitted .beads/ state") — whose explicit scope is staging these traces.
- Four trace beads are still **in_progress** (bf-3cu1k, bf-5wku, bf-gj673, bf-j7w7) and two traces are
  **in-flight** (bf-32451 = this session; bf-fwx5y = active sibling, metadata still empty). Committing
  them now would capture partial/growing files and race with their owners.

Each trace is committed by the worker that closes its owning bead (the established repo pattern:
582 trace files are already tracked). This bead's deliverable is the classification above; it commits
only this `notes/bf-32451.md`.
