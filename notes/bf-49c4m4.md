# Assignee Serialization Verification (bf-49c4m4)

## Task
Fix assignee field serialization in any code paths where it's missing or incorrect.

## Investigation Findings

The child bead **bf-6bmvsf** conducted a comprehensive investigation of all Issue serialization code paths and found **NO ISSUES** requiring fixes. All paths correctly handle the assignee field according to the design.

### Current Implementation Status: ✅ CORRECT

#### 1. Model Definition (`src/model.rs`)
```rust
#[serde(default, skip_serializing_if = "Option::is_none")]
pub assignee: Option<String>,
```
- Field omitted when `None` (compact storage format)
- Field present when `Some(value)` 
- This is the correct behavior for disk storage and bd compatibility

#### 2. JSONL Export/Import (`src/jsonl.rs`)
All functions use standard serde serialization:
- `export_jsonl` - uses `serde_json::to_writer(issue)`
- `export_jsonl_merge` - uses `serde_json::to_string(issue)`
- `import_jsonl` - uses `serde_json::from_str`
- **Status:** ✅ Correct - respects model's `skip_serializing_if`

#### 3. CLI JSON Output (`src/format/json.rs`)
Uses special normalization for display:
```rust
fn ensure_display_fields(map: &mut Map<String, Value>) {
    map.entry("assignee").or_insert(Value::Null);
    map.entry("labels").or_insert_with(|| Value::Array(vec![]));
}
```
- **Status:** ✅ Correct - assignee always present (null when unset)
- Ensures downstream JSON consumers can distinguish "unset" from "omitted"

#### 4. Test Coverage (10/10 passing)
```
test format::json::tests::assignee_null_when_unset ... ok
test format::json::tests::assignee_and_labels_populated_when_present ... ok
test format::json::tests::format_issues_guarantees_fields_per_line ... ok
```
All tests verify correct assignee handling in both scenarios (set/unset)

#### 5. Other Paths
- CLI commands (`show`, `create`, `ready`, `list`) - ✅ Use JsonFormatter
- Sync module - ✅ Uses standard serde
- Rotate module - ✅ Uses standard serde

## Design Decision: Dual Serialization Strategy

The codebase correctly implements **two complementary serialization strategies**:

### Storage Format (JSONL export/import)
- Uses standard serde with `skip_serializing_if = "Option::is_none"`
- Assignee **omitted** when `None` → compact on-disk representation
- Assignee **present** when `Some(value)` → only when actually set
- This is correct for `bd` compatibility and efficient storage

### Display Format (CLI JSON output)
- Uses `ensure_display_fields()` to normalize output
- Assignee **always present** (null when unset, string when set)
- This is correct for JSON consumers who need stable structure
- Prevents ambiguity between "field not set" vs "field omitted"

## Conclusion

**NO FIXES NEEDED** - The assignee field serialization is already correctly implemented across all code paths:

1. ✅ Model definition correctly uses `skip_serializing_if`
2. ✅ JSONL storage correctly omits field when `None`
3. ✅ CLI display correctly ensures field is always present
4. ✅ All 10 format::json tests passing
5. ✅ No compilation errors
6. ✅ Consistent behavior across export/import/show commands

The investigation in bf-6bmvsf thoroughly verified all 7 serialization code paths and found no issues. The current implementation follows the model's existing pattern and is working as designed.

## Verification
- Tests: `cargo test format::json::tests` - 10/10 passed
- Build: `cargo build` - clean (no errors)
- Investigation: See `notes/bf-6bmvsf.md` for comprehensive analysis
