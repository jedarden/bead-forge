# bf-54ul: verify --help outputs usage with commands and flags

Task: run `bf --help` and confirm it renders a `Usage:` header, lists the
available commands, and shows the available flags/options in a readable,
formatted layout.

## Verification

Ran `bf --help` (installed binary, `bf 0.3.0`) and checked each acceptance
criterion against the captured output:

- **`Usage:` header** — present: `Usage: bf [OPTIONS] [COMMAND]`.
- **Lists available commands** — present. The `Commands:` section enumerates
  all **32** subcommands (create, list, show, update, close, reopen, delete,
  ready, claim, init, sync, doctor, commit-check, count, batch, mitosis, dep,
  label, labels, comments, search, stats, schema, config, velocity, annotate,
  log, critical-path, rotate, migrate, recent, help), each with a one-line
  description.
- **Shows available flags/options** — present. The `Options:` section lists
  `-w, --workspace <WORKSPACE>`, `-h, --help`, and `-V, --version`, each with
  a description.
- **Readable and formatted** — yes. Output has a description preamble, aligned
  two-column layout under `Commands:` and `Options:`, and exits 0.

## Result

No source changes required. `bf --help` already satisfies every acceptance
criterion — the help is auto-generated by clap from the doc comments already
audited in sibling beads (notably `bf-1pbp`).
