# bf-4zln: verify --help returns exit code 0

Task: run `bf --help`, capture its exit code, and confirm it is exactly 0
(the command succeeds without errors).

## Verification

Ran `bf --help` from the installed binary (`/home/coding/.local/bin/bf`,
`bf 0.3.0`) and captured the exit code:

```
$ bf --help; echo $?
bead-forge - Drop-in replacement for beads_rust (br)
Usage: bf [OPTIONS] [COMMAND]
Commands:
  create     Create a new bead
  ...
  help       Print this message or the help of the given subcommand(s)
Options:
  -w, --workspace <WORKSPACE>  ...
  -h, --help                   Print help
  -V, --version                Print version
0
```

- **Exit code** — exactly `0` (captured via `echo $?`).
- **No errors** — the command emits the standard clap help to stdout and
  produces nothing on stderr. Help text renders fully: a description
  preamble, `Usage:` header, the `Commands:` list (all 32 subcommands), and
  the `Options:` section (`-w`, `-h`, `-V`).
- **Binary provenance** — `/home/coding/.local/bin/bf`, the real installed
  `bf`, not a shell alias or shim.

## Result

No source changes required. `bf --help` exits 0 and renders successfully —
the help is auto-generated by clap from the doc comments already audited in
sibling beads (notably `bf-1pbp` and `bf-54ul`).
