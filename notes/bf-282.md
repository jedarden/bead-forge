# bf-282: Build bf binary and test --version flag

## Task
Compile the bf binary and verify that the `--version` flag works correctly.

## What was done
- Ran `cargo build` — completed cleanly with no errors.
- Verified the binary exists at `target/debug/bf`.
- Ran `./target/debug/bf --version`.

## Result
```
$ ./target/debug/bf --version
bf 0.3.0
```

- Output includes `bf` ✓
- Output includes the version number `0.3.0` ✓
- Format is correct: `bf 0.3.0` ✓
- Exit code: 0 ✓
- Version matches `version = "0.3.0"` in `Cargo.toml`.

No source changes were required — this was a build-and-verify task only.
