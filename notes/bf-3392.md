# bf-3392: Test --version flag output

## Implementation Status: ✅ VERIFIED (no code changes needed)

`./target/debug/bf --version` correctly prints `bf 0.3.0` and exits 0. The output
includes the program name `bf` and the version number `0.3.0`, which matches the
`version` field in `Cargo.toml`. No source changes were required.

## Task

Run `./target/debug/bf --version` and verify the output includes `bf` and the
correct version number (e.g., `bf 0.2.0`).

## Verification

```bash
$ grep -E "^version" Cargo.toml | head -1
version = "0.3.0"

$ ./target/debug/bf --version; echo "exit code: $?"
bf 0.3.0
exit code: 0
```

| # | Criterion | Result |
|---|-----------|--------|
| 1 | Output includes `bf` | ✅ `bf 0.3.0` |
| 2 | Version number is correct | ✅ `0.3.0` matches `Cargo.toml` `version = "0.3.0"` |
| 3 | Exits cleanly | ✅ exit code 0 |

clap's `Parser` derive wires up `-V`/`--version` automatically from
`#[command(name = "bf")]` combined with the crate version, so the canonical
`<name> <version>` format is emitted for free (see `bf-3rwg` for the broader
clap derive verification).

## Conclusion

All criteria pass. `bf --version` prints `bf 0.3.0` and exits 0. No bugs found,
no code changes needed.
